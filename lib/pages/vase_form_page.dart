import 'package:flutter/material.dart';

class VaseFormPage extends StatelessWidget {
  const VaseFormPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text('화분만들기'),
    );
  }
}
